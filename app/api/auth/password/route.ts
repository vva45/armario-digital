import { NextResponse } from "next/server";
import { authenticatedSupabase } from "../../../../data/supabase/server";
export async function POST(request: Request) { const auth = await authenticatedSupabase(); if (auth.state !== "authenticated") return NextResponse.json({ error: auth.state === "unconfigured" ? "Conexión pendiente" : "Inicia sesión" }, { status: 401 }); const { password } = await request.json(); if (typeof password !== "string" || password.length < 10) return NextResponse.json({ error: "La contraseña debe tener al menos 10 caracteres." }, { status: 400 }); const response = await auth.request("/auth/v1/user", { method: "PUT", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ password }) }); return response.ok ? NextResponse.json({ ok: true }) : NextResponse.json({ error: "No se pudo cambiar la contraseña." }, { status: response.status }); }
