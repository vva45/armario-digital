"use client";
import { useEffect, useState, type FormEvent } from "react";
import { useRouter } from "next/navigation";
export function AuthPanel() { const router = useRouter(); const [mode,setMode]=useState<"login"|"recover">("login"); const [message,setMessage]=useState(""); const [busy,setBusy]=useState(false); async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);setMessage("");const data=new FormData(event.currentTarget);const response=await fetch(`/api/auth/${mode}`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email:data.get("email"),password:data.get("password")})});const body=await response.json();setBusy(false);if(!response.ok)return setMessage(body.error);if(mode==="recover")return setMessage("Si la cuenta existe, recibirás un enlace para recuperar el acceso.");router.push("/armario/salir");router.refresh();} return <form className="auth-card" onSubmit={submit}><p className="eyebrow">Acceso privado</p><h1>{mode==="login"?"Inicia sesión":"Recupera el acceso"}</h1><p>{mode==="login"?"Tu inventario solo está disponible con una sesión válida.":"Te enviaremos el flujo oficial de recuperación a tu email."}</p><label>Email<input name="email" type="email" autoComplete="email" required /></label>{mode==="login"&&<label>Contraseña<input name="password" type="password" autoComplete="current-password" required /></label>}{message&&<p role="status" className="form-error">{message}</p>}<button className="button" disabled={busy}>{busy?"Comprobando…":mode==="login"?"Entrar":"Enviar enlace"}</button><button className="text-button" type="button" onClick={()=>{setMode(mode==="login"?"recover":"login");setMessage("")}}>{mode==="login"?"He olvidado la contraseña":"Volver al inicio de sesión"}</button></form> }
export function SessionControl(){const router=useRouter();const [email,setEmail]=useState<string>();useEffect(()=>{fetch('/api/auth/session').then(r=>r.json()).then(d=>setEmail(d.email))},[]);if(!email)return null;return <button className="text-button" onClick={async()=>{await fetch('/api/auth/logout',{method:'POST'});router.push('/acceso')}}>Salir ({email})</button>}
